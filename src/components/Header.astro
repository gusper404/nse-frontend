---
const navlinks = [
  ['Inicio', '/' ],
  ['Quiénes somos', '/quienes-somos' ],
  ['Radio', '/radio' ],
  ['TV', '/tv' ],
  ['Cine', '/cine' ],
  ['Contacto', '/contacto' ]
]
---

<header class="navbar bg-white border-b">
  <div class="navbar-start">
    <img src="/logo-nse.png" alt="Logo NSE Internacional"></a>
  </div>
  <div class="navbar-center hidden lg:flex">
    <ul class="menu menu-horizontal px-1 text-black">
      {navlinks.map(([title, url]) => (
        <li><a href={url} class="uppercase">{title}</a></li>
      ))}
    </ul>
  </div>
  <div class="navbar-end">
    <a target="_blank" rel="noopeer" href="https://www.paypal.com/donate/?cmd=_s-xclick&hosted_button_id=2XUCGTB2QA9DC" class="btn hidden lg:flex bg-red-700 hover:bg-red-800 border-0 text-white">DONAR</a>
    <div class="dropdown dropdown-left dropdown-bottom">
      <div tabindex="0" role="button" class="btn btn-ghost text-black lg:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" /></svg>
      </div>
      <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52">
        {navlinks.map(([title, url]) => (
          <li><a href={url}>{title}</a></li>
        ))}
      </ul>
    </div>
  </div>
</header>