---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";

const { image, imgw, imgh, title, url, icon, target } = Astro.props;
---

<div class="bg-white shadow-md rounded-lg overflow-hidden">
  <a href={url} target={target}>
    <figure>
      <Image width={imgw} height={imgh} class="w-full aspect-video object-cover" src={image} alt={title} />
    </figure>
  </a>
  <div class="p-2 md:p-4">
    {icon && (
      <div class="inline-flex items-center gap-2 text-slate-500">
        <Icon name={icon} />
        <span class="text-xs uppercase">{icon === 'tv' ? 'televisión' : icon}</span>
      </div>
    )}
    <a target={target} href={url} rel="noopener" class="font-semibold text-sm md:text-lg text-primary line-clamp-1">{title}</a>
  </div>
</div>